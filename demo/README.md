# Demo

The program prefers "a short demo video ... A pitch, live demo or reproducible walkthrough also
works."

**Today:** the reproducible walkthrough in
[`../docs/04-product-demo.md`](../docs/04-product-demo.md#reproducible-walkthrough) — a reviewer
builds the contract and runs its tests from a clean checkout in three commands.

**Milestone 2** adds a live testnet walkthrough: registering and resolving a handle via
`qubic-cli`, with a recorded terminal session here.

**Milestone 3** adds the artifact that matters most — sending a testnet transaction to a name
instead of a 60-character address, in a real wallet UI.
